
# === Entorno ===
NODE_ENV=development
PORT=85

# === Docker / MySQL ===
MYSQL_ROOT_PASSWORD=cambia_esto
MYSQL_USER=robeurope_user
MYSQL_PASSWORD=cambia_esto
MYSQL_HOST_PORT=3306
REDIS_HOST_PORT=6379

# === Base de datos — desarrollo ===
DB_HOST_DEV=localhost
DB_PORT_DEV=3306
DB_NAME_DEV=robeurope_dev
DB_USER_DEV=robeurope_user
DB_PASS_DEV=cambia_esto

# === Base de datos — test ===
DB_HOST_TEST=localhost
DB_PORT_TEST=3306
DB_NAME_TEST=robeurope_test
DB_USER_TEST=robeurope_user
DB_PASS_TEST=cambia_esto

# === Base de datos — producción ===
DB_HOST_PROD=localhost
DB_PORT_PROD=3306
DB_NAME_PROD=robeurope_prod
DB_USER_PROD=robeurope_user
DB_PASS_PROD=cambia_esto

# === Redis ===
REDIS_URL=redis://localhost:6379

# === Seguridad (usa: openssl rand -base64 64) ===
SESSION_SECRET=cambia_esto_por_algo_largo_y_aleatorio
JWT_SECRET=cambia_esto_por_algo_largo_y_aleatorio

# === Dominio ===
TEAM_DOMAIN=robeurope.samuelponce.es
# COOKIE_DOMAIN=.robeurope.samuelponce.es   ← descomentar en producción

# === Frontend ===
VITE_API_BASE_URL=http://localhost:85
VITE_WS_URL=ws://localhost:85
VITE_APP_NAME="RobEurope Dev"
VITE_TEAM_DOMAIN=robeurope.samuelponce.es

# === OAuth ===
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# === Email (Resend) ===
RESEND_API_KEY=

# === Push notifications (genera con: npx web-push generate-vapid-keys --json) ===
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# === Seeders ===
SEED_SUPERADMIN_PASSWORD=Admin123!
SEED_CENTER_ADMIN_PASSWORD=CenterAdmin123!
SEED_STUDENT_PASSWORD=Student123!
