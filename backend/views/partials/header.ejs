<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> | RobEurope Admin</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@1.5.10/css/pico.min.css" />
  <style>
    :root {
      --primary: #2563eb; /* Solid blue */
      --primary-hover: #1d4ed8;
      --background: #ffffff; /* Light background */
      --surface: #f8fafc; /* Light surface */
      --text: #0f172a; /* Dark text */
      --text-muted: #64748b;
      --border: #e2e8f0;
    }

    @media (prefers-color-scheme: dark) {
      :root {
        --primary: #3b82f6;
        --primary-hover: #60a5fa;
        --background: #0f172a; /* Dark background */
        --surface: #1e293b; /* Dark surface */
        --text: #f1f5f9; /* Light text */
        --text-muted: #94a3b8;
        --border: #334155;
      }
    }

    * {
      box-sizing: border-box;
    }

    body {
      background: var(--background);
      color: var(--text);
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      min-height: 100vh;
      display: flex;
      margin: 0;
    }

    /* Sidebar */
    .sidebar {
      width: 250px;
      background: var(--surface);
      border-right: 1px solid var(--border);
      display: flex;
      flex-direction: column;
      position: fixed;
      height: 100vh;
      overflow-y: auto;
      transition: transform 0.3s ease;
      z-index: 100;
    }

    .sidebar-header {
      padding: 1.5rem;
      border-bottom: 1px solid var(--border);
    }

    .sidebar-brand {
      font-size: 1.1rem;
      font-weight: 600;
      color: var(--text);
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .sidebar-nav {
      padding: 1rem 0.75rem;
      flex: 1;
    }

    .nav-item {
      display: block;
      padding: 0.6rem 0.75rem;
      color: var(--text-muted);
      text-decoration: none;
      border-radius: 6px;
      margin-bottom: 0.1rem;
      transition: all 0.2s ease;
      font-size: 0.9rem;
      font-weight: 500;
    }

    .nav-item:hover, .nav-item.active {
      background: var(--background);
      color: var(--primary);
      font-weight: 600;
    }
    
    /* In dark mode, hover background should be lighter than surface, or just different */
    @media (prefers-color-scheme: dark) {
        .nav-item:hover, .nav-item.active {
            background: rgba(255, 255, 255, 0.05);
        }
    }

    .sidebar-footer {
      padding: 1rem;
      border-top: 1px solid var(--border);
    }

    .user-info {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      padding: 0.5rem;
    }

    .user-avatar {
      width: 32px;
      height: 32px;
      background: var(--primary);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 600;
      color: white;
      font-size: 0.85rem;
    }

    /* Main Content */
    .main-content {
      flex: 1;
      margin-left: 250px;
      padding: 2rem;
      width: calc(100% - 250px);
    }

    /* Mobile Toggle */
    .menu-toggle {
      display: none;
      position: fixed;
      top: 1rem;
      right: 1rem;
      z-index: 200;
      background: var(--surface);
      border: 1px solid var(--border);
      padding: 0.5rem;
      border-radius: 6px;
      color: var(--text);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .sidebar {
        transform: translateX(-100%);
      }
      
      .sidebar.open {
        transform: translateX(0);
      }

      .main-content {
        margin-left: 0;
        width: 100%;
        padding: 1rem;
        padding-top: 4rem;
      }

      .menu-toggle {
        display: block;
      }
    }

    /* Common Components */
    h2 {
      color: var(--text);
      margin-bottom: 1.5rem;
      font-size: 1.5rem;
      font-weight: 600;
      letter-spacing: -0.025em;
    }

    .card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 1.5rem;
      box-shadow: none;
      margin-bottom: 1.5rem;
    }

    /* Dashboard Grid */
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      margin-bottom: 2rem;
    }

    .stat {
      padding: 1.5rem;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 8px;
      text-align: left;
      transition: none;
      box-shadow: none;
    }

    .stat:hover {
      transform: none;
      border-color: var(--primary);
    }

    .stat h3 {
      margin: 0 0 0.5rem;
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--text-muted);
      font-weight: 600;
    }

    .stat p {
      margin: 0;
      font-size: 1.8rem;
      font-weight: 600;
      color: var(--text);
      background: none;
      -webkit-text-fill-color: initial;
    }

    /* Charts */
    .charts-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
      gap: 1.5rem;
      margin-bottom: 2rem;
    }

    .chart-wrapper {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 1.5rem;
    }

    /* Tables */
    .table-container {
      overflow-x: auto;
      background: var(--surface);
      border-radius: 8px;
      border: 1px solid var(--border);
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin: 0;
    }

    table th {
      background: var(--background);
      color: var(--text-muted);
      font-weight: 600;
      text-transform: uppercase;
      font-size: 0.75rem;
      padding: 0.75rem 1rem;
      text-align: left;
      border-bottom: 1px solid var(--border);
    }

    table td {
      padding: 0.75rem 1rem;
      border-bottom: 1px solid var(--border);
      color: var(--text);
      font-size: 0.9rem;
    }

    table tr:last-child td {
      border-bottom: none;
    }

    table tr:hover {
      background: rgba(0, 0, 0, 0.02);
    }
    
    @media (prefers-color-scheme: dark) {
        table tr:hover {
            background: rgba(255, 255, 255, 0.02);
        }
    }

    /* Buttons */
    .btn {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      font-weight: 500;
      font-size: 0.9rem;
      text-decoration: none;
      transition: all 0.2s ease;
      border: 1px solid transparent;
      cursor: pointer;
    }

    .btn-primary {
      background: var(--primary);
      color: white;
    }

    .btn-primary:hover {
      background: var(--primary-hover);
    }

    .btn-danger {
      background: #ef4444;
      color: white;
    }
    
    .btn-danger:hover {
        background: #dc2626;
    }

    .btn-sm {
      padding: 0.25rem 0.5rem;
      font-size: 0.8rem;
    }

    /* Forms */
    input, select, textarea {
      background: var(--surface) !important;
      border: 1px solid var(--border) !important;
      color: var(--text) !important;
      border-radius: 6px !important;
      padding: 0.5rem 0.75rem !important;
      font-size: 0.9rem !important;
    }

    input:focus, select:focus, textarea:focus {
      border-color: var(--primary) !important;
      box-shadow: 0 0 0 2px rgba(37, 99, 235, 0.1) !important;
      outline: none !important;
    }

    /* Badges */
    .badge {
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .badge-success { background: rgba(16, 185, 129, 0.1); color: #10b981; }
    .badge-warning { background: rgba(245, 158, 11, 0.1); color: #f59e0b; }
    .badge-danger { background: rgba(239, 68, 68, 0.1); color: #ef4444; }
    .badge-info { background: rgba(59, 130, 246, 0.1); color: #3b82f6; }

    /* D3 Tooltip */
    .d3-tooltip {
      position: absolute;
      padding: 0.5rem 0.75rem;
      background: var(--surface);
      color: var(--text);
      border: 1px solid var(--border);
      border-radius: 6px;
      pointer-events: none;
      font-size: 0.85rem;
      z-index: 1000;
      opacity: 0;
      transition: opacity 0.3s;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body>
  <% if (currentUser) { %>
    <button class="menu-toggle" onclick="document.querySelector('.sidebar').classList.toggle('open')">
      ☰
    </button>

    <aside class="sidebar">
      <div class="sidebar-header">
        <div class="sidebar-brand">
           RobEurope Admin
        </div>
      </div>
      
      <nav class="sidebar-nav">
        <a href="/admin" class="nav-item <%= title === 'Admin Dashboard' ? 'active' : '' %>">
           Dashboard
        </a>
        <a href="/admin/users" class="nav-item <%= title === 'Users' || title === 'Editar Usuario' ? 'active' : '' %>">
           Usuarios
        </a>
        <a href="/admin/competitions" class="nav-item <%= title === 'Competiciones' ? 'active' : '' %>">
           Competiciones
        </a>
        <a href="/admin/registrations" class="nav-item <%= title === 'Registrations' ? 'active' : '' %>">
           Solicitudes
        </a>
        <a href="/admin/teams" class="nav-item <%= title === 'Equipos' ? 'active' : '' %>">
           Equipos
        </a>
        <a href="/admin/posts" class="nav-item <%= title === 'Posts' ? 'active' : '' %>">
           Posts
        </a>
        <a href="/admin/sponsors" class="nav-item <%= title === 'Patrocinadores' ? 'active' : '' %>">
           Patrocinadores
        </a>
        <a href="/admin/countries" class="nav-item <%= title === 'Países' ? 'active' : '' %>">
           Países
        </a>
        <a href="/admin/logs" class="nav-item <%= title === 'Logs del Sistema' ? 'active' : '' %>">
           Logs
        </a>
        <a href="/admin/ldap-users" class="nav-item <%= title === 'Usuarios LDAP' ? 'active' : '' %>">
           Usuarios LDAP
        </a>
      </nav>

      <div class="sidebar-footer">
        <div class="user-info">
          <div class="user-avatar">
            <%= (currentUser && currentUser.username) ? currentUser.username.charAt(0).toUpperCase() : 'A' %>
          </div>
          <div style="flex: 1; overflow: hidden;">
            <div style="font-weight: bold; font-size: 0.9rem; white-space: nowrap; overflow: hidden; text-overflow: ellipsis;">
              <%= (currentUser && currentUser.username) ? currentUser.username : 'Admin' %>
            </div>
            <a href="/admin/logout" style="color: var(--text-muted); font-size: 0.8rem; text-decoration: none;">
              Cerrar Sesión
            </a>
          </div>
        </div>
      </div>
    </aside>

    <main class="main-content">
  <% } else { %>
    <main style="width: 100%; padding: 2rem; display: flex; align-items: center; justify-content: center; min-height: 100vh;">
  <% } %>
