<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> | RobEurope Admin</title>
  <link rel="stylesheet" href="/css/admin.css" />
</head>
<body>
  <header class="app-header">
    <nav class="nav-bar">
      <div class="brand">RobEurope Admin</div>
      <div class="nav-links">
      <% if (currentUser) { %>
        <a href="/admin" class="nav-link">Dashboard</a>
        <a href="/admin/users" class="nav-link">Users</a>
        <a href="/admin/logout" class="nav-link">Logout (<%= currentUser.username || currentUser.email %>)</a>
      <% } else { %>
        <a href="/admin/login" class="nav-link">Login</a>
      <% } %>
      </div>
    </nav>
  </header>
  <main class="container page-container">
    <%- body %>
  </main>
  <footer class="footer">RobEurope Admin &copy; <%= new Date().getFullYear() %></footer>
  <script src="/vendor/d3.min.js"></script>
  <script src="/js/dashboard-chart.js" defer></script>
</body>
</html>