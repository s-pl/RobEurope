# Forward DNS Lookup
This document describes the **forward DNS lookup** configuration for the domain:

## Description

This document describes the **forward DNS lookup** configuration for the domain:
46.101.255.106

The domain is also configured with a **CNAME record** pointing to: XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX (on this case we dont have a CNAME record)

This indicates that the domain is hosted on **Vercel**, which manages the final IP resolution.


## How to Perform the Forward Lookup

You can verify the forward lookup using the following command:
```bash
nslookup api.robeurope.samuelponce.es
```

## Forward Lookup Evidence
![IMAGEN-LOOKUP](https://github.com/s-pl/RobEurope/blob/LXLLXVX-patch-2/img/lookup.png?raw=true)
